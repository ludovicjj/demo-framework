{% extends '@admin/base_admin.html.twig' %}
{% block body %}
    <h1 class="mb-5">Editer {{ item.name }}</h1>

    <form method="post" action="{{ path('admin.posts.edit', {id: item.id}) }}">
        {% include '@admin/post/form_post.html.twig' %}
        <button class="btn btn-primary" type="submit">Editer</button>
    </form>

{% endblock %}